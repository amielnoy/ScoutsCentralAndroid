<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  android:fillViewport="true"
  android:background="#F8F9FA"
  android:layoutDirection="rtl">

  <androidx.constraintlayout.widget.ConstraintLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp">

    <TextView
      android:id="@+id/dashboard_title"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:text="לוח מחוונים"
      android:textSize="26sp"
      android:textStyle="bold"
      android:textColor="#1A1C1E"
      app:layout_constraintTop_toTopOf="parent"
      app:layout_constraintStart_toStartOf="parent" />

    <TextView
      android:id="@+id/dashboard_subtitle"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:text="ברוכים השבים! הנה סיכום מצב השבט שלכם."
      android:textColor="#44474E"
      android:textSize="14sp"
      android:layout_marginTop="4dp"
      app:layout_constraintTop_toBottomOf="@id/dashboard_title"
      app:layout_constraintStart_toStartOf="parent" />

    <com.google.android.material.button.MaterialButton
      android:id="@+id/sync_now_button"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:text="סנכרון"
      app:icon="@android:drawable/stat_notify_sync"
      style="@style/Widget.MaterialComponents.Button.TextButton"
      app:layout_constraintTop_toTopOf="parent"
      app:layout_constraintEnd_toEndOf="parent" />

    <!-- Statistics Section in 2x2 Grid -->
    <androidx.gridlayout.widget.GridLayout
      android:id="@+id/dashboard_stats"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:layout_marginTop="20dp"
      app:columnCount="2"
      app:rowCount="2"
      app:useDefaultMargins="true"
      app:layout_constraintTop_toBottomOf="@id/dashboard_subtitle"
      app:layout_constraintStart_toStartOf="parent"
      app:layout_constraintEnd_toEndOf="parent">
      <!-- 4 Cards will be added here: Top-Right, Top-Left, Bottom-Right, Bottom-Left -->
    </androidx.gridlayout.widget.GridLayout>

    <TextView
      android:id="@+id/section_activities_title"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:text="@string/soon_activitys"
      android:textStyle="bold"
      android:textSize="18sp"
      android:textColor="#1A1C1E"
      android:layout_marginTop="32dp"
      app:layout_constraintTop_toBottomOf="@id/dashboard_stats"
      app:layout_constraintStart_toStartOf="parent" />

    <androidx.recyclerview.widget.RecyclerView
      android:id="@+id/upcoming_list"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:layout_marginTop="12dp"
      android:nestedScrollingEnabled="false"
      app:layout_constraintTop_toBottomOf="@id/section_activities_title"
      app:layout_constraintStart_toStartOf="parent"
      app:layout_constraintEnd_toEndOf="parent" />

    <TextView
      android:id="@+id/section_announcements_title"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:text="הודעות אחרונות"
      android:textStyle="bold"
      android:textSize="18sp"
      android:textColor="#1A1C1E"
      android:layout_marginTop="32dp"
      app:layout_constraintTop_toBottomOf="@id/upcoming_list"
      app:layout_constraintStart_toStartOf="parent" />

    <androidx.recyclerview.widget.RecyclerView
      android:id="@+id/announcement_list"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:layout_marginTop="12dp"
      android:nestedScrollingEnabled="false"
      app:layout_constraintTop_toBottomOf="@id/section_announcements_title"
      app:layout_constraintStart_toStartOf="parent"
      app:layout_constraintEnd_toEndOf="parent"
      android:paddingBottom="24dp" />

  </androidx.constraintlayout.widget.ConstraintLayout>
</ScrollView>
